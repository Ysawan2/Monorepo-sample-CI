on:
  push:
    branches: ["actions-v1"]
    paths: 
      - "backend/*"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v4
      - name: build backend
        working-directory: ./backend
        run: go build .